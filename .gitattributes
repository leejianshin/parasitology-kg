# Deterministic generated text artifacts are compared byte-for-byte.
# Keep their canonical LF bytes on every operating system.
derived/**/*.md text eol=lf
derived/**/*.yml text eol=lf
derived/**/*.jsonl text eol=lf
derived/**/*.csv text eol=lf

